worktree:
  branch-template: "{parent-slug}/{slug}"
  path-template: "../{parent-slug}-{slug}"

models:
  session: opus
  plan-review: sonnet
  code-review: sonnet
  explore: haiku

notifications:
  slack:
    enabled: false
    webhook: ""
    channel: ""
  toast:
    enabled: true
