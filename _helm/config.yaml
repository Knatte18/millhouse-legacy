worktree:
  branch-template: "{parent-branch}-wt-{slug}"
  path-template: "../{repo-name}-wt-{slug}"

models:
  session: opus
  plan-review: sonnet
  code-review: sonnet
  explore: haiku

notifications:
  slack:
    enabled: false
    webhook: ""
    channel: ""
  toast:
    enabled: true
