# Millhouse

## Backlog

### Bare-repo hub-struktur [backlog]

    ```md
    git clone --bare + worktrees. Automatiser som /clone-hub skill. Vurder støtte i /worktree og helm-start.
    ```

### helm-spawn.ps1 [backlog]

    ```md
    PS-script for worktree setup. git worktree add, kanban-filer, status.md, .vscode/settings.json (unik farge + window.title). Erstatter CC-basert spawning i helm-start.
    ```

### git /git-pr skill [backlog]

    ```md
    Merge til main med PR. Pull origin/main inn i nåværende branch, resolve conflicts, verifiser, push, lag PR (eller direct merge basert på config). Ny skill i git-pluginen.
    ```
